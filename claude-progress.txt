# Omnismi Progress Log

Session checklist
- Read `app_spec.txt`, `feature_list.json`, and `docs/agent-playbook.md`.
- Run `./init.sh` to refresh the editable install and baseline checks.
- Prefer one small feature, validation task, or contract-tightening change per session.
- Update both this file and `feature_list.json` before finishing.

Open issues
- Real hardware validation is still sparse beyond NVIDIA H20 and AMD MI300X.
- `mkdocs` content exists, but docs build verification is not yet part of the default automated harness.
- Future vendor expansion must preserve the current minimal API shape.

Entries
- 2026-03-12: Bootstrapped an Anthropic-style long-running agent harness for this repo. Added `app_spec.txt`, `feature_list.json`, `init.sh`, and documented the workflow in agent/development docs. Baseline checks passed with `PYTHONPATH=src pytest -q` and `python -m compileall src tests`.
- 2026-03-12: Added AMD cached sampling and `GPU.realtime()` parity with the NVIDIA backend, documented `utilization_percent` as a cross-vendor activity signal instead of SM/CU occupancy, and moved the README quick start closer to the top. Validation passed with `PYTHONPATH=src pytest -q` and `python -m compileall src tests`.
- 2026-03-12: Added an experimental Google TPU backend based on `libtpu.sdk.tpumonitoring`, updated install/docs/compatibility metadata, and recorded the current limitation that TPU has snapshot metrics but no direct parity collector yet.
- 2026-04-22: Expanded the roadmap toward trustable hardware diagnostics and validation. Added explicit planning for CLI diagnostics, machine profile/spec validation, and portable benchmark probes, updated `docs/roadmap.md`, recorded new planned items in `feature_list.json`, and refreshed `app_spec.txt`. Validation passed with `python -m json.tool feature_list.json > /dev/null` and `PYTHONPATH=src pytest -q`.
- 2026-04-23: Added a planned `omnismi bench` CLI design doc covering subcommands, shared flags, and a versioned `table/json/yaml` report contract in `docs/bench.md`, wired it into `mkdocs.yml`, and recorded the structured-output direction in `feature_list.json` and `app_spec.txt`. Validation passed with `python -m json.tool feature_list.json > /dev/null` and `PYTHONPATH=src pytest -q`; `mkdocs build --strict` could not run because `mkdocs` is not installed in the current environment.
- 2026-04-23: Added a planned top-level CLI design in `docs/cli.md` covering `omnismi` default overview behavior, `overview`/`doctor`/`validate-spec` relationships, a modern cross-vendor terminal summary, and explicit host/container/Kubernetes discovery expectations. Updated `docs/compatibility.md`, `docs/roadmap.md`, `app_spec.txt`, `feature_list.json`, and `mkdocs.yml` to record the container-aware discovery contract and the goal that Omnismi should match runtime-visible GPUs where supported. Validation passed with `python -m json.tool feature_list.json > /dev/null` and `PYTHONPATH=src pytest -q`.
- 2026-04-23: Refined the human-first CLI design with concrete terminal mockups and rendering rules. Expanded `docs/cli.md` to define the main-entry behavior for `omnismi`, the detail ladder across `overview --wide`/`doctor`/`bench`, narrow-terminal behavior, and explicit example outputs for host, container, no-GPU, and mismatch scenarios. Updated `app_spec.txt` and `feature_list.json` to record that the main entry should stay human-first by default, with explicit `json`/`yaml` output on demand. Validation passed with `python -m json.tool feature_list.json > /dev/null` and `PYTHONPATH=src pytest -q`.
- 2026-04-23: Simplified the CLI design so the public discovery entry is just `omnismi`, without a redundant `omnismi overview` command. Updated `docs/cli.md`, `docs/bench.md`, `docs/roadmap.md`, `docs/index.md`, `app_spec.txt`, and `feature_list.json` so `--wide` is treated as an expansion of the main entry rather than a separate subcommand path. Validation passed with `python -m json.tool feature_list.json > /dev/null` and `PYTHONPATH=src pytest -q`.
- 2026-04-23: Implemented the first stable CLI slice for discovery. Added `src/omnismi/cli.py`, `src/omnismi/__main__.py`, the `omnismi` script entrypoint, structured `OverviewReport` output in `table/json/yaml`, and tests for default, wide, filtered, empty, and structured output flows in `tests/test_cli_overview.py`. Updated README and CLI docs to reflect the implemented main discovery command and marked `cli-overview` as verified in `feature_list.json`. Validation passed with `PYTHONPATH=src pytest -q`, `PYTHONPATH=src pytest -q tests/test_cli_overview.py`, `PYTHONPATH=src python -m omnismi -o json`, and `python -m json.tool feature_list.json > /dev/null`.
- 2026-04-23: Implemented `omnismi doctor` as the first diagnostics layer on top of the overview report. Added plain-language findings, possible causes, next steps, structured `DoctorReport` output, and tests for visibility mismatch, partial metrics, and JSON output in `tests/test_cli_overview.py`. Updated README, CLI docs, and `feature_list.json` to mark backend diagnostics as verified. Validation passed with `PYTHONPATH=src pytest -q`, `PYTHONPATH=src python -m omnismi doctor -o json`, and `python -m json.tool feature_list.json > /dev/null`.
- 2026-04-23: Polished the human-facing CLI presentation for both `omnismi` and `omnismi doctor`. Added denser, dashboard-style layout blocks, adaptive column reduction for tighter terminals, explicit `--color auto|always|never` support with `--no-color` aliasing, optional network-rate sampling when psutil is available, and updated CLI docs/examples to match the implemented renderer. Validation passed with `PYTHONPATH=src pytest -q`, `PYTHONPATH=src python -m omnismi --color always`, `PYTHONPATH=src python -m omnismi doctor --color always`, and `python -m json.tool feature_list.json > /dev/null`.
- 2026-04-23: Completed the first runtime-visibility parity slice for discovery and diagnostics. Added explicit `visibility_scope` and `visibility_controls` fields to CLI environment reporting, surfaced runtime-scoped filters in `omnismi --wide` and `omnismi doctor`, and added tests that cover host-global, container, Kubernetes, and env-filtered discovery expectations. Updated compatibility/spec docs and marked `environment-discovery-parity` as verified in `feature_list.json`. Validation passed with `PYTHONPATH=src pytest -q`, `PYTHONPATH=src python -m omnismi --wide --no-color`, `PYTHONPATH=src python -m omnismi doctor --no-color`, and `python -m json.tool feature_list.json > /dev/null`.
- 2026-04-23: Implemented the first `omnismi validate-spec` slice together with a small curated machine-profile registry. Added built-in profiles for `h100-pcie-80gb`, `mi300x-192gb`, and `tpu-v5p-32gb`, wired `validate-spec` into the CLI with `table/json/yaml` output and `PASS/WARN/FAIL/INCONCLUSIVE` verdicts, and documented how this profile-based validation differs from the existing vendor-library parity checker. Validation passed with `PYTHONPATH=src pytest -q`, `PYTHONPATH=src python -m omnismi validate-spec --profile h100-pcie-80gb -o json`, `PYTHONPATH=src python -m omnismi validate-spec --profile unknown-sku --no-color`, and `python -m json.tool feature_list.json > /dev/null`.
